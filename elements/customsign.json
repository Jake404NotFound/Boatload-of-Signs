{
  "id": "customsign",
  "modelement": true,
  "type": "block",
  "icon": "sign",
  "name": "Custom Sign",
  "description": "Create custom signs with custom textures and properties",
  "category": "blocks",
  "fields": {
    "name": {
      "type": "string",
      "name": "Name",
      "description": "The name of the sign",
      "required": true
    },
    "texture": {
      "type": "texture",
      "name": "Sign Texture",
      "description": "The texture for the sign",
      "required": true
    },
    "textureBack": {
      "type": "texture",
      "name": "Back Texture (optional)",
      "description": "The texture for the back of the sign",
      "required": false
    },
    "material": {
      "type": "string",
      "name": "Material",
      "description": "The material of the sign",
      "default": "WOOD",
      "required": true
    },
    "waterloggable": {
      "type": "boolean",
      "name": "Waterloggable",
      "description": "Whether the sign can be waterlogged",
      "default": true,
      "required": false
    },
    "lightLevel": {
      "type": "number",
      "name": "Light Level",
      "description": "The light level emitted by the sign",
      "min": 0,
      "max": 15,
      "default": 0,
      "required": false
    },
    "requiredTool": {
      "type": "string",
      "name": "Required Tool",
      "description": "The tool required to break the sign",
      "default": "AXE",
      "required": false
    }
  }
}
